# -*- python -*-

import sys

Import('env')

libs = ['moppe-map', 'moppe-gfx', 'moppe-mov']

if sys.platform != 'darwin':
    libs.append('glut')

env.Program('moppe', ['main.cc', 'app.cc', 'gl.cc'],
            LIBS=libs,
            LIBPATH='..')
